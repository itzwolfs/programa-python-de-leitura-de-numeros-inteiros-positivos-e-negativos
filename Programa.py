#primeiro iremos colocar o n1, para ser a nossa base de indentifica
#logo em seguida vamos usar o int(input ('dentro colocaremos o que irá aparecer na tela do python
#para encerra iremos colocar o if apontando o nosso numero com o simbolo menor que 1
#abaixo da variante if iremos colocar o print ('voce colocar o que irá aparecer no logo sendo print if menor que zero e else maior que zero')
n1 = int(input('Digite o primero numero: ')
if n1 < 0:
    print('add')
else:
    print('add')
#isso é para te dar uma base e apoio porém voce pode deixar ainda melhor com loops adicionando o novo print embaixo do else
#espero ter ajudado em sua base de de exercicio ou aprendizado basico em py
